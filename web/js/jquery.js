$(function () {
        const today = parseInt(new Date().getHours());
        if (today > 22 || today < 6) {
            $('body').addClass('bg-dark');
        }
    }
)

$(document).ready(function () {
const today = parseInt(new Date().getHours());
        if (today > 22 || today < 6) {
            $('body').addClass('bg-dark');
        }
    }
)
